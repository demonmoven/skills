# Runtime Behavior

本文件描述系统的关键运行时行为——数据流、状态变化、异步流程。
仅在仓库包含异步/流式/事件驱动逻辑时生成此文档。

## 核心数据流

<!-- HARNESS-GEN: 从 Phase 1 依赖图分析 + 用户访谈中提取核心数据流。
     描述从用户操作（或外部触发）到最终结果的完整路径。
     使用 Mermaid 或 ASCII 图表示主要数据流方向。
     如果 Phase 1 未能完全确定，标记不确定部分为 Knowledge Gap。 -->

## 状态管理

<!-- HARNESS-GEN: 从代码模式采样中提取状态管理方式。
     包括：
     - 全局状态的存储位置和访问方式
     - 状态变更的触发机制
     - 状态持久化策略（如有）
     - 状态同步机制（多端/多窗口场景，如有）
     如果 Phase 1 未能确定，标记为 Knowledge Gap。 -->

## 异步与实时通信

<!-- HARNESS-GEN: 从 Phase 1 外部集成点分析中提取异步通信模式。
     仅在检测到 SSE/WebSocket/消息队列/长轮询等模式时填写。
     包括：
     - 使用的通信协议和库
     - 连接生命周期管理（建立、心跳、重连）
     - 错误恢复和降级策略
     - 消息序列化格式
     如果 Phase 1 未检测到异步通信模式，删除此节或标记为 N/A。 -->

## 关键生命周期

<!-- HARNESS-GEN: 从代码模式采样中提取应用的关键生命周期事件。
     例如：
     - 应用启动流程（初始化顺序、依赖加载）
     - 请求处理管道（中间件链、拦截器）
     - 优雅关闭流程（资源清理、连接断开）
     如果 Phase 1 未能确定，标记为 Knowledge Gap。 -->
