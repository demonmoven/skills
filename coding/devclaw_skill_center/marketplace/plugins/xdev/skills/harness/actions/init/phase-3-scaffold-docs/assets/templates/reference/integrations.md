# External Integrations

本文件记录项目与外部系统/服务的集成关系。
仅在仓库有外部服务/API 集成时生成此文档。

## 集成清单

<!-- HARNESS-GEN: 从 Phase 1 外部集成点分析中提取集成清单。
     扫描 HTTP client 初始化、环境变量引用、配置文件中的外部 URL/服务名、
     SDK 初始化代码，为每个外部集成填写一行。
     如果某些列信息无法从代码中确定，标记对应单元格为"待确认"并记录 Knowledge Gap。 -->

| 服务 | 用途 | 接入方式 | 配置位置 | 故障降级 |
|------|------|----------|----------|----------|
| <!-- HARNESS-GEN: 外部服务名称 --> | <!-- HARNESS-GEN: 该服务在本项目中的用途 --> | <!-- HARNESS-GEN: SDK/REST API/gRPC/GraphQL 等 --> | <!-- HARNESS-GEN: 环境变量名或配置文件路径 --> | <!-- HARNESS-GEN: 该服务不可用时的降级策略，如无则标记"待确认" --> |

## 环境变量清单

<!-- HARNESS-GEN: 从 Phase 1 外部集成点分析中提取所有与外部服务相关的环境变量。
     列出变量名、用途、是否必须、默认值（如有）。
     如果 Phase 1 未能完全确定，标记为 Knowledge Gap。 -->

| 变量名 | 用途 | 必须 | 默认值 |
|--------|------|------|--------|
| <!-- HARNESS-GEN: 从环境变量扫描中提取 --> | | | |

## 集成架构图

<!-- HARNESS-GEN: 基于上方集成清单，使用 Mermaid 绘制本项目与外部服务的集成关系图。
     标注数据流方向和通信协议。
     如果集成关系简单（少于 3 个外部服务），可省略此节。 -->
