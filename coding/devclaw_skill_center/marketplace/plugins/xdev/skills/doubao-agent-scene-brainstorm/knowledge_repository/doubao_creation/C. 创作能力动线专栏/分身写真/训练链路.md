# 分身写真 — 训练链路（独立任务化链路）

> ⚪ **骨架占位**
>
> **业务 PM**：陈思颖
> **入口特点**：**任务化长链路**（数据采集 → 训练 → 验收 → 上线）；与 chat 触发模式完全不同；可能在独立的 PSM / 队列消费 / FaaS 中

---

## 状态

- [ ] 用户动线（包含数据采集 → 训练任务提交 → 进度查询 → 完成通知）
- [ ] PSM 链路（可能涉及 `creation_queue_consumer` / `creativity_video_process_faas` / `creativity_asset_faas_message` 等仓库）
- [ ] 训练任务模型 / 数据存储
- [ ] 与本 skill 主 scope 的边界

## 关键差异

- 与"用 chat 触发"的推理链路完全不同
- 可能不走 `flow.agent.creation`
- 可能不在本 skill 8 仓库内（涉及 creativity_asset / creation_queue_consumer 等）

## scope 提示

如果你的需求涉及分身**训练链路**，本 skill 可能 scope 不够，建议：
1. 看 `marketplace/plugins/xdev/scenes/doubao.json` 列出的 14 仓库（含训练相关 FaaS）
2. 考虑改用 `harness-kit` 类工具做 setup
3. 推理链路相关需求才完全在本 skill scope 内

## TODO

需求触达时补充。考虑是否触发"另立 skill"的决策。
