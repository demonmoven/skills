# A Code Clean Tool for Go

## Intergation Testing

The Core Tools of Go Cleaner use llvm-lit for integration testing. Before running the tests, you need to install the environment via pip3 and use the script command to execute the tests:

```bash
pip3 install lit filecheck
./test/test.sh
```

(Tested on Cloud IDE)